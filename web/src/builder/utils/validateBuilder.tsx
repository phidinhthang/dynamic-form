import { BuilderCtx } from '../builderReducer';

// validate nonexisted child id
// shaking unuse element id
// check key must exist
// must have unique key
// check validation max > min
// type

export const validateBuilder = ({ elements, layout }: BuilderCtx) => {
  return true;
};
